namespace TheCube
{
    /// <summary>
    /// An enum representing the type of piece, currently not used for 2x2 because all pieces
    /// are corners.
    /// </summary>
    public enum Types
    {
        Corner = 'c',
        Edge = 'e',
        Center = 'm'
    }
}